import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center'
  },
  tokensContainer: {
    flexDirection: 'row'
  },
  tokenWrapper: {
    marginLeft: -10,
    zIndex: 1
  },
  firstToken: {
    marginLeft: 0
  }
});
