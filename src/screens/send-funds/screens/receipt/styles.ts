import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerContentMiddle: {
    alignItems: 'center'
  },
  walletSelector: {
    backgroundColor: 'transparent',
    padding: 0
  }
});
