import { Text, View } from 'react-native';

export const ManageAccountsScreen = () => {
  return (
    <View
      style={{
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignContent: 'center'
      }}
    >
      <Text style={{ textAlign: 'center' }}> MANAGE-ACCOUNTS</Text>
    </View>
  );
};
