import { Text, View } from 'react-native';

export const PreferencesScreen = () => {
  return (
    <View
      style={{
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignContent: 'center'
      }}
    >
      <Text style={{ textAlign: 'center' }}> PREFERENCES</Text>
    </View>
  );
};
